<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/settings_scroll_view"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:fitsSystemWindows="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="32dp">

        <!-- Header -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingTop="24dp"
                android:paddingBottom="24dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="软件设置"
                    android:textColor="@color/text_main"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.02" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="外观与主题偏好"
                    android:textColor="@color/text_main"
                    android:alpha="0.45"
                    android:textSize="13sp" />
            </LinearLayout>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_back"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_gravity="start|top"
                android:layout_marginTop="6dp"
                app:icon="@drawable/ic_arrow_back" />
        </FrameLayout>

        <!-- ========== 外观模式（弹窗选择） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_display_mode" />

        <!-- ========== 主题配色（弹窗选择） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_theme_color" />

        <!-- ========== 主界面刷新频率（弹窗选择） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_refresh_interval" />

        <!-- ========== 指定 Wi-Fi 下刷新（弹窗设置） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_wifi_lock" />

        <!-- ========== 省电暂停：纯开关语义，直接在入口右侧给开关 ========== -->
        <include
            layout="@layout/layout_common_switch_item"
            android:id="@+id/item_pause_screen_off" />

        <include
            layout="@layout/layout_common_switch_item"
            android:id="@+id/item_pause_power_save" />



        <!-- ========== 自定义背景图片 (暂时隐藏入口) ==========
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_bg_image" />
        -->

    </LinearLayout>
</ScrollView>
