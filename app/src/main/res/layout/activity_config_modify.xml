<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:fitsSystemWindows="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="32dp">

        <!-- ========== Header ========== -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingTop="24dp"
                android:paddingBottom="24dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="连接配置"
                    android:textColor="@color/text_main"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.02" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="修改 UFI 设备连接参数与 API 接口"
                    android:textColor="@color/text_main"
                    android:alpha="0.45"
                    android:textSize="13sp" />
            </LinearLayout>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_back"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_gravity="start|top"
                android:layout_marginTop="6dp"
                app:icon="@drawable/ic_arrow_back" />
        </FrameLayout>

        <!-- ========== 设备配置档（多设备切换，放最上面：它决定下面几项改的是谁） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_device_profile" />

        <!-- ========== 数据源（弹窗选择：UFI-TOOLS / Goform 直连） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_data_source" />

        <!-- ========== 基础连接（弹窗编辑：地址 + 口令） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_basic_config" />

        <!-- ========== 高级配置（弹窗编辑：API 路径 + 密钥） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_advanced_config" />

    </LinearLayout>
</ScrollView>
