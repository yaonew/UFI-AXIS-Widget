<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:fitsSystemWindows="true"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingBottom="32dp">

        <!-- Header -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="16dp"
            android:layout_marginTop="0dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:orientation="vertical"
                android:paddingTop="24dp"
                android:paddingBottom="24dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="小组件动态配色"
                    android:textColor="@color/text_main"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.02" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="从小组件背景图提取色调，自动适配文字颜色"
                    android:textColor="@color/text_main"
                    android:alpha="0.45"
                    android:textSize="13sp" />
            </LinearLayout>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_back"
                style="@style/Widget.Material3.Button.IconButton"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:layout_gravity="start|top"
                android:layout_marginTop="6dp"
                app:icon="@drawable/ic_arrow_back"
                app:iconTint="@color/text_main" />
        </FrameLayout>

        <!-- ========== 外观作用域：正在设置哪个组件（桌面上没有组件时整块隐藏） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_appearance_scope"
            android:visibility="gone" />

        <include
            layout="@layout/layout_common_switch_item"
            android:id="@+id/item_appearance_override"
            android:visibility="gone" />

        <!-- ========== 动态配色 Material You（开关，仅 API 31+ 显示） ========== -->
        <include
            layout="@layout/layout_common_switch_item"
            android:id="@+id/item_widget_dynamic_color" />

        <!-- ========== 动态配色对比度（弹窗选择，仅动态配色开启时显示） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_widget_dynamic_contrast"
            android:visibility="gone" />

        <!-- ========== 动态配色高级设置（弹窗，仅动态配色开启时显示） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_widget_dynamic_advanced"
            android:visibility="gone" />

        <!-- ========== 动态配色色源选择（弹窗，仅动态配色开启时显示） ========== -->
        <include
            layout="@layout/layout_common_setting_item"
            android:id="@+id/item_widget_dynamic_color_source"
            android:visibility="gone" />

    </LinearLayout>
</ScrollView>
